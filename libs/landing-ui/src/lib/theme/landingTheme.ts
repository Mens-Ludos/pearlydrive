import { baseTheme } from '@pearlydrive/core-ui';

import { colors } from './colors';
import type { LandingTheme } from './types';

export const landingTheme: LandingTheme = {
  ...baseTheme,
  colors,
};
